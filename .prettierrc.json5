{
  tabWidth: 4,
  // git should be set to only commit lf
  // ci will check its in lf, leave this in auto during dev
  endOfLine: "auto",
  overrides: [
    {
      files: "*.{json,json5,yml,yaml}",
      options: {
        tabWidth: 2,
      },
    },
  ],
}
